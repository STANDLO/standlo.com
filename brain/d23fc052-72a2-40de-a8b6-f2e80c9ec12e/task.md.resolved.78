# Task Plan: Refactoring Button Component

- [x] Rimuovi dal component Button le vecchie mappature per `primary`, `secondary`, `destructive`, `ghost`, `link` mantenendo solo un fallback all'outline.
- [x] Verifica tutti gli utilizzi di `variant="primary"` o simili e sostituiscili preferibilmente col tema base `default` per non rompere il sito.
- [x] Fai una passata per cercare se ci sono versioni Muted/Readonly di base theme passate con template class.es to [globals.css](file:///Users/cristiankalexai/Progetti/standlo.com/src/app/globals.css) se richiesto dal nuovo design.

## Refactoring Execution
- [x] Rimuovere tutte le classi Tailwind inline dal file `ToolsNavigator.tsx`.
- [x] Definire le corrispondenti classi semantiche in [globals.css](file:///Users/cristiankalexai/Progetti/standlo.com/src/app/globals.css) utilizzando il prefisso `ui-tools-navigator`.
- [x] Assicurarsi che nessuna classe Tailwind rimanga in `ToolsNavigator.tsx`.
- [x] Applicare le nuove classi semantiche a tutti gli elementi per uno stile coerente.
- [x] Verificare che il componente funzioni correttamente (inclusa la correzione dell'errore di sintassi).

## Cleanup & Fix
- [x] Rimuovere il fix "chiusura" del NextThemeProvider (hardcoded dark/light palette su .dark e .light) per ripristinare il corretto override del background e color-foreground base di Tailwind.
- [x] Fix Hydration Mismatch su [Button.tsx](file:///Users/cristiankalexai/Progetti/standlo.com/src/components/ui/Button.tsx): rimosso l'uso server-incompatible di `useTheme()`, aggiungendo il fallback statico `variant="default"`.
- [x] Risolvere gli errori di build di TailwindCSS causati da utilities sconosciute (`fade-in`, `zoom-in`, `animate-in`).
- [x] Sostituire l'animazione `animate-in` nel dropdown con CSS keyframes nativi.
- [x] Aggiungere l'effetto "glue" all'evento hover del bottone "Registrati" in [globals.css](file:///Users/cristiankalexai/Progetti/standlo.com/src/app/globals.css) mixando `--color-glue` al base layer `--primary`.
- [x] Sostituire il pulsante `<button>` nativo in `ToolsNavigator` con il `<Button variant="ghost" size="icon">` dell'UI.
  - [x] Remove unknown Tailwind classes causing build errors (`animate-in`, `fade-in`, etc.).
  - [x] Replace native `<button>` with `<Button>` component for the menu toggle.
  - [x] Fix the registration button hover effect by correctly using `color-mix()` with `var(--color-primary)` instead of raw `var(--primary)` HSL numbers to enable the glue 3D effect.
- [x] Verificare che il processo di build (`npm run build`) abbia successo.` component.
- [x] Update `ToolsNavigator` registration button to use the `green` variant.
- [x] Refactor all color themes (`.theme-green` to `.green` etc) in [globals.css](file:///Users/cristiankalexai/Progetti/standlo.com/src/app/globals.css) to provide the complete set of structural layout CSS variables while centralizing actual HSL variables.
- [x] Refactor all `ui-btn-[color]` classes to use semantic variables instead of hardcoded tailwind colors (Button variants now map `green` -> `ui-btn-theme theme-green`, replacing explicit classes with inherited `--primary` vars).
- [x] Implemented `--glue` CSS variable per theme (black for light, white for dark/colors) and centralized translucency effect on buttons hover, logo glue, and post-it card header.
- [x] Correct `.ui-card-footer` styling (width, padding, gap) to align properly with contents.
- [x] Update translations for `orDivider` and `loginDividerText` in [es.json](file:///Users/cristiankalexai/Progetti/standlo.com/messages/es.json), [de.json](file:///Users/cristiankalexai/Progetti/standlo.com/messages/de.json), [fr.json](file:///Users/cristiankalexai/Progetti/standlo.com/messages/fr.json), [en.json](file:///Users/cristiankalexai/Progetti/standlo.com/messages/en.json), [us.json](file:///Users/cristiankalexai/Progetti/standlo.com/messages/us.json) and [it.json](file:///Users/cristiankalexai/Progetti/standlo.com/messages/it.json).

## Theme Architecture Stabilization (v3)
- [x] Rename `.green`, `.blue`, `.yellow`, `.fucsia`, `.violet`, `.red`, `.orange`, `.dark`, and `.light` to `.theme-*` in [globals.css](file:///Users/cristiankalexai/Progetti/standlo.com/src/app/globals.css)
- [x] Remove global vars from all `.theme-*` blocks in [globals.css](file:///Users/cristiankalexai/Progetti/standlo.com/src/app/globals.css) so they inherit from HTML root.
- [x] Update `Card.tsx` to apply `theme-${color}` instead of `${color}` for post-it variants.
- [x] Ensure `HomeOverlay.tsx` static colors keep working smoothly.
- [x] Replace `next-themes` with custom cookie-based `ThemeProvider` to eliminate hydration mismatch and ensure correct static background rendering.
- [x] Re-test theme switcher (Dark/Light on HTML root) against colored POST-ITs.

## POST-IT Design System
- [x] Refactor `Card` component in [src/components/ui/Card.tsx](file:///Users/cristiankalexai/Progetti/standlo.com/src/components/ui/Card.tsx) to handle `title`, `icon`, [action](file:///Users/cristiankalexai/Progetti/standlo.com/src/components/layout/canvas/GenericPart.tsx#148-191), and `footer` props directly.
  - [x] Add `layout` prop (`'full' | 'auto'`) to `Card` to control sizing (full-width/height vs auto-sized & centered).
- [x] Implement centered title (`fullwidth`), absolute left icon, and absolute right action layout in `CardHeader`.
- [x] Replace `CardAuth` with explicit `Card` configuration (`layout="auto"`) in `FormLogin` and `FormRegister`, extracting `CardAuthSocials` to its own component.
- [x] Create layout components: `ToolsMain.tsx`, `ToolsMainSidebar.tsx`, `ToolsMainContent.tsx`, `ToolsPropbar.tsx`, and `ToolsFooter.tsx` in `src/components/layout/tools/`.
  - [x] Configure `ToolsMainSidebar` and `ToolsPropbar` to be collapsible, defaulting to collapsed (width: 64px).
  - [x] Configure `ToolsFooter` to be fixed at the bottom, height: 64px, left/right 0.

## Gestione SwitchMode (Overlay Globali)
- [x] Aggiungi i model (`tools` e `canvas`) in `functions/src/index.ts` affinché non interferiscano con Firebase-Admin.
- [x] Crea `src/components/ui/SwitchMode.tsx` per cambiare e salvare il cookie `ui_mode`.
- [x] Modifica `src/app/[locale]/layout.tsx` per leggere il cookie `ui_mode` (default `tools`) dalla request server.
- [x] Sposta la props `active` su `ToolsOverlay.tsx` in modo che renderizzi i children diretti quando disattivato.
- [x] Inserisci `<SwitchMode />` in `ToolsNavigator.tsx` accanto a `<SwitchLocale />`.
- [x] Estrazione di `BaseLogo` e `BaseNavigator` per renderizzazione globale persistente al di fuori delle Overlays (Canvas / Tools). Aggiunto parametro `color` in `functions/src/index.ts` per l'icona mode.
- [x] Update `ToolsOverlay.tsx` to accept `children` and use `ToolsMain` to render them inside `ToolsMainContent`.
- [x] Remove `layout-auth-page` wrappers from `auth/login/page.tsx` and `auth/create/page.tsx`.

## Gestione Bugfix Cloud Deploy
- [x] Refactoring di `xrStore` in un modulo isolato (`xrStore.ts`) per rimuovere la dipendenza sincrona da `page.tsx` durante l'import dinamico del Canvas.
- [x] Correzione della logica di override su `CanvasOverlay` e `ToolsOverlay` (lato client) per ignorare il cookie `ui_mode` quando si atterra su `/partner/[roleId]/canvas`.
- [x] Fix aggiuntivo su `src/app/[locale]/layout.tsx` che forza l'apertura del Canvas per gli utenti in variante `public` (non autenticati) per garantire il funzionamento della landing page.
- [x] Modificato il fallback di default del cookie `ui_mode` da `tools` a `canvas` in modo che i nuovi utenti e il pubblico vedano prima il design 3D. Aggiunta validazione dei permessi (`hasToolsAccess`) per nascondere la voce `tools` dallo SwitchMode per gli utenti non autorizzati.

## Canvas Onboarding & PDM Layers
- [x] Schema `canvas.ts`: Aggiungere `canvasType`, definizioni `layerId`, e forzare UIDs a `crypto.randomUUID()`. Aggiungere field per `editPassword` (hash).
- [x] Schema `part.ts` / `assembly.ts`: Aggiungere il metadato `useCases: { canvasType: string, canvasLayer: string }[]` per l'auto-routing deterministico.
- [x] Endpoint `createCanvasSandbox`: Creare callable HTTP (con AppCheck middleware) che istanzia il canvas di prova `public` o per utente loggato.
- [x] UI Onboarding Home Page: Modificare `src/app/[locale]/page.tsx` esportando il render sul nuovo componente guidato `CanvasOnboarding`.
- [x] UI Esperienza Avanzata: Implementare Drag & Drop Sidebar, Fading Layers inattivi e il Tutorial Guidato.
- [x] URL Condivisione e Protezione Edit: Creare la rotta `src/app/[locale]/canvas/public/[uid]/page.tsx` con protezione password per i poteri di modifica (Role Editor).
- [x] Canvas Claiming (Registrazione): Implementare la logica in AuthEdge/Post-Login per trasferire la proprietà (Update `orgId`/`userId`) del Canvas pubblico all'account appena creato.
- [x] PDM Endpoint Orchestrator (`canvas-insert-node`): Callable HTTP (AppCheck protetto) "fire and forget" che avvia la Choreography asincrona per copiare entità PDM nel canvas.
- [x] Inserimento (Optimistic UI): Aggiungere la logica drag&drop/click dei componenti dal Catalogo, con push immediato nello Zunstand store instradato dal metadato `useCases`.

## Refactoring 3D Canvas Editor
- [x] Store improvements: added new state variables `transformMode`, `cameraMode`, `shadingMode`, etc. in `store.ts`.
- [x] Interactive Object Manipulation: Refactored `GenericPart.tsx` to use the new transform and snapping logic.
- [x] UI enhancements: Added Toolbar for switching tools (move, rotate, snap) and modes (orthographic/perspective, shading options).
- [x] Replicate Admin Editor mechanics: Implemented `MutedAxes`, grid options, origin snapping, and snap indicators in `Canvas.tsx`, mirroring `AssemblyCanvasEditor.tsx`.

## Full Ecosystem Validation
- [x] Root: 0 Lint Errors/Warnings, 0 Build Errors, 0 Vulnerabilities
- [x] Admin: 0 Lint Errors/Warnings, 0 Build Errors, 0 Vulnerabilities
- [x] Functions: 0 Lint Errors/Warnings, 0 Build Errors, 0 Vulnerabilities
- [x] Resolving Cloud Deployment canvas dynamic import chunking crash.

## Integrazione HomeOverlay e systemUiModes
- [x] Aggiungere `home` mode come vista di default all'interno di `systemUiModes` in `functions/src/index.ts`.
- [x] Modificare il cookie default da `canvas` a `home` in `src/app/[locale]/layout.tsx`.
- [x] Creare il componente `src/components/layout/HomeOverlay.tsx`.
- [x] Ripristinare il contenuto precedente di `ui-home` in `src/app/[locale]/page.tsx` avvolgendolo dentro `HomeOverlay` o renderizzandolo dalla route.
- [x] Configurare l'azione `/canvas` (es. tramite bottone) per attivare la modalità `canvas` (impostare cookie) nascondendo la `HomeOverlay` e mostrando la `CanvasOverlay`.

## HomeOverlay Design a Griglia (8x8)
- [x] Modificare `globals.css` definendo `.ui-home` come `grid grid-cols-8 grid-rows-8 gap-0` (con fallback responsivo) e `.ui-home-cell` con `p-8`.
- [x] Impostare le colonne e righe 3 e 6 ad una frazione dimezzata (0.5fr vs 1fr) in `globals.css` per comprimere la griglia centrale.
- [x] Rendere completamente trasparente il background del componente Home rimuovendo gli sfondi e bordi superflui del grid.
- [x] Forzare il `defaultTheme` su `"light"` nell'`AppProviders.tsx` bloccando il binding con il System Theme dell'OS.
- [x] Definire un'animazione di entrata "staggered" (a cascata) in `globals.css` combinata a una leggera rotazione al passaggio del mouse per i componenti Card (effetto Post-It reale).
- [x] Modificare `src/components/ui/Card.tsx` per rimuovere il malfunzionante `theme-${color}` usando stringhe dirette e includere `"dark"` e `"light"` nel tipo `CardColor`.
- [x] Risignare `HomeOverlay.tsx`: Rimuovere il `<Logo />` e inserire le 9 `HomeCard` native alle corrette coordinate `col-start` e `row-start` con `col-span-2 row-span-2`.
- [x] Inserire componenti strutturali ausiliari temporanei (`HomeSync` e `HomeSyncGrid`) con iconografia direzionale e riempitiva per i vuoti.
- [x] Rendere cliccabili le Cards all'interno dell'HomeOverlay (chiusura Overlay o routing in base al contesto).

## Bugfix & Stabilizzazione Canvas Sandbox
- [x] Fix Hydration Mismatch e errore `asChild` nativo in `Button.tsx` (temporaneamente convertito il wrapper in `src/app/[locale]/canvas/public/[uid]/page.tsx` a un standard Link con utility classes).
- [x] Correzione `Canvas.tsx` e `CanvasOverlay.tsx` in modo che renderizzi la sandbox evaluando `entityType = "canvas"` (invece che "stand").
- [x] Refactoring backend `createCanvasSandbox`, `claimCanvasSandbox` e `insertNode` (in `canvas.ts`) per scrivere le identità globali sotto la root `canvases/` del DB `standlo` anziché l'obsoleta path nesting in `organizations`.
- [x] Modifica dell'`entityRegistry.ts` per supportare il path building delle subcollections (es. `canvas/{id}/objects` mappato a `canvases/{id}/objects`) in modo che l'Orchestrator possa eseguire regolarmente interrogazioni `list` sul catalogo mesh 3D associate.

## Canvas Tools Component (Nuovo)
- [x] Create `CanvasTools.tsx` with floating sidebar layout.
- [x] Implement **"Inserimento"** block (Parts, Assembly, Bundles, Stand).
- [x] Implement **"Azioni"** block (Translate, Rotate, Snap).
- [x] Link "Azioni" buttons to `useCanvasStore.setTransformMode()`.
- [x] Implement **"View"** block (3D, AR, VR placeholders).
- [x] Implement **"Manufacturing"** block (BoM, BoP).
- [x] Implement **"Share"** block with Popover.
- [x] Implement Sandbox Password Logic (`editPassword`) generation and `?key=` URL consumption.
- [x] Convert `CanvasTools` inline Tailwind classes into semantic (`.ui-canvas-tools*`) variables in `globals.css` and arrange them perfectly horizontal (`top-4 left-32`).
- [x] Conceal the "Editor Sbloccato" modal entirely once the canvas is unlocked, ensuring the viewport is devoted purely to the models and the new horizontal `CanvasTools` dashboard.
- [x] Moved `CanvasTools` directly into `Canvas.tsx` to ensure it is always globally visible, and removed the old overlapping "Sola Lettura / Editor Sbloccato" modal entirely from `page.tsx`.
- [x] Extracted button styles from CanvasTools to `BaseNavigator` inner actions (`SwitchLocale`, `SwitchTheme`, `SwitchMode`, `profile`, `support`) to ensure unified UI rendering.
- [x] Group structural styling for CanvasTools updated with strict sizes (64px, 54px).
- [x] Dynamic internationalisation of texts matching system locale injected in JSONs.

## Auth & Security Refinements
- [x] Fixed issue where `orgId` was being set to `null` instead of `"system"` during entity creation for administrative users without a rigid tenant. Updated `create` handler in `functions/src/gateways/firestore.ts` to automatically fallback to `"system"`.
