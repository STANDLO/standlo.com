# Consolidating API Client Architecture to Orchestrator-Only

- [/] Audit the codebase for legacy API calls
  - [ ] Search for `httpsCallable` usages outside of [api.ts](file:///Users/cristiankalexai/Progetti/standlo.com/src/lib/api.ts).
  - [ ] Search for raw URL fetches to the emulator (`http://127.0.0.1:5001/`).
  - [ ] Search for [callGateway](file:///Users/cristiankalexai/Progetti/standlo.com/src/lib/api.ts#14-50) invokes targeting specific gateways instead of `orchestrator`.
- [ ] Evaluate `src/app/[locale]/(public)/auth` exceptions.
- [ ] Present the list of files needing updates to the user (via `implementation_plan.md`).
- [ ] Refactor all identified files to use `callGateway("orchestrator", { entityId, actionId, payload })`.
- [ ] Verify functionality via build / tests.
